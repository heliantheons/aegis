apiVersion: v1
kind: Service
metadata:
  name: aegis
  labels:
    app.kubernetes.io/name: aegis
    app.kubernetes.io/part-of: heliantheon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aegis
  ports:
    - name: http
      port: 18000
      targetPort: http

